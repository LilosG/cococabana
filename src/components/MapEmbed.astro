---
interface Props {
  src: string;
  title: string;
  class?: string;
}

const { src, title, class: className = 'w-full h-full border-0' } = Astro.props;
---

<iframe
  src={src}
  title={title}
  loading="lazy"
  referrerpolicy="strict-origin-when-cross-origin"
  class={className}
></iframe>
